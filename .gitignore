# Binaries
/vrm
/vrmd
/bin/
*.exe
*.dll
*.so
*.dylib

# Test & coverage output
# Deliberately no bare *.out — testdata/ fixtures are load-bearing here (spec §14)
# and must never be silently ignored.
*.test
coverage.out
coverage.html
*.prof

# Go workspace files (per-developer, never shared)
go.work
go.work.sum

# Secrets and local config. .env.example IS committed.
.env
.env.*
!.env.example
*.pem
*.key
config.local.yaml

# Claude Code local overrides (.claude/settings.json is committed)
.claude/settings.local.json

# Editors / OS
.idea/
.vscode/
*.swp
*~
.DS_Store

# Project
resume-session.txt
